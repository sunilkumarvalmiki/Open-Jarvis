# Clippy configuration for Open-Jarvis

# Deny warnings in CI
# Use: cargo clippy -- -D warnings

# Allowed lints (use sparingly)
# These are lints we explicitly allow in our codebase

# Warn on common mistakes
# These will be warnings, not errors

# Additional lints to check
# Run with: cargo clippy -- -W clippy::pedantic
